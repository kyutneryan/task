import { StyleSheet } from 'react-native'
import { GHOST_WHITE_COLOR } from '../../constants/colors'

export const styles = StyleSheet.create({
  safeAreaView: {
    flex: 1,
  },
  root: {
    backgroundColor: GHOST_WHITE_COLOR,
  },
})
