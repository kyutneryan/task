import { StyleSheet } from 'react-native'
import { DARK_BLUE_COLOR, GRAY_COLOR } from '../../../constants/colors'

export const styles = StyleSheet.create({
  input: {
    height: 40,
    margin: 12,
    padding: 10,
    borderWidth: 1,
    borderColor: GRAY_COLOR,
    borderRadius: 6,
    color: DARK_BLUE_COLOR,
  },
})
